[package]
name = "heatsync-core"
version = "0.1.0"
edition = "2021"

# the brain: protocol types, heat ramp, emote resolution, ws parsing.
# ZERO terminal/async/io deps — TUI and CLI are thin faces over this.
[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
